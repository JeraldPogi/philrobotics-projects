#include <platform.h>
#include <N6100LCD.h>

int main(void)
{
	Serial_begin(115200);
	//n6100lcd_init(DAT_PIN, CLK_PIN, CS_PIN, RST_PIN)
	n6100lcd_init(PA0, PA2, PA3, PA1);
	n6100lcd_setArea(0,131,0,131);
    
	while(true)
	{
		n6100lcd_send(1, Serial_readbyte() );
	}
}
