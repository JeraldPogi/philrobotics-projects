void pinToggle()
{
	togglePin(LED1);
	togglePin(LED2);
	togglePin(LED3);
	togglePin(LED4);
}

void init()
{
	setupUserInt(INT0, pinToggle, CHANGE);
	clrPin(LED1);
	clrPin(LED2);
	clrPin(LED3);
	clrPin(LED4);
}

void program()
{
	/* do nothing */
}
